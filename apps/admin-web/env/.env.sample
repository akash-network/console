# Deployment environment (development, staging, production)
DEPLOYMENT_ENV=development

# ============================================================================
# Auth0 Configuration
# ============================================================================
# IMPORTANT: Create a SEPARATE Auth0 application for admin-web (not the same as deploy-web).
# This provides security isolation and allows independent configuration of callbacks,
# logout URLs, and user access. Both apps use the same Auth0 TENANT but different CLIENT IDs.
#
# In Auth0 Dashboard → Applications → Create Application:
#   - Name: "Akash Console Admin" (or similar)
#   - Type: Regular Web Application
#
# Configure the application with:
#   - Allowed Callback URLs: http://localhost:3011/api/auth/callback
#   - Allowed Logout URLs: http://localhost:3011
#   - Allowed Web Origins: http://localhost:3011

# Required: Secret used to encrypt the session cookie (generate a random 32+ character string)
AUTH0_SECRET=

# Required: The base URL of this application (admin-web runs on port 3011)
AUTH0_BASE_URL=http://localhost:3011

# Required: Your Auth0 domain (same tenant as deploy-web, e.g., https://your-tenant.auth0.com)
AUTH0_ISSUER_BASE_URL=

# Required: Your Auth0 application client ID (DIFFERENT from deploy-web - use admin app's client ID)
AUTH0_CLIENT_ID=

# Required: Your Auth0 application client secret (DIFFERENT from deploy-web - use admin app's secret)
AUTH0_CLIENT_SECRET=

# Optional: Auth0 API audience (can be the same API as deploy-web - JWT validation still works)
AUTH0_AUDIENCE=

# Auth0 SDK - login route (optional, defaults to /api/auth/login)
NEXT_PUBLIC_AUTH0_LOGIN=/login

# ============================================================================
# Admin API Configuration
# ============================================================================
# Server-side URL to the admin-api service (used by the proxy route)
ADMIN_API_URL=

# ============================================================================
# Optional Configuration
# ============================================================================
# App version displayed in sidebar (optional)
NEXT_PUBLIC_APP_VERSION=
