# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB_URI=postgresql://postgres:password@localhost:5432/console-users

# ============================================================================
# Server Configuration
# ============================================================================
PORT=3010
CORS_WEBSITE_URLS=http://localhost:3011

# ============================================================================
# Auth0 JWT Verification
# ============================================================================
# Uses the same Auth0 TENANT as deploy-web (tokens from any app in the tenant are valid).
# admin-web should use a SEPARATE Auth0 APPLICATION (different client ID) for security isolation,
# but JWT validation uses the tenant-level JWKS endpoint which works for all apps in the tenant.

# Required: JWKS endpoint for token verification (tenant-level, works for all apps)
AUTH0_JWKS_URI=https://your-tenant.auth0.com/.well-known/jwks.json

# Required: Token issuer (your Auth0 domain with trailing slash)
AUTH0_ISSUER=https://your-tenant.auth0.com/

# Optional: Expected audience claim (can be same API as deploy-web or a dedicated admin API)
AUTH0_AUDIENCE=

# ============================================================================
# Admin Access Control
# ============================================================================
# Domain-based restriction: Only users with emails from these domains can access
# Multiple domains can be comma-separated: akash.network,example.com
ADMIN_ALLOWED_DOMAINS=akash.network

# Email whitelist: Specific emails to allow regardless of domain
# Multiple emails can be comma-separated: admin@other.com,dev@external.org
ADMIN_WHITELIST_EMAILS=

# ============================================================================
# Logging Configuration
# ============================================================================
LOG_LEVEL=info
STD_OUT_LOG_FORMAT=pretty
NODE_ENV=development
