"use client";
import type { FunctionComponent } from "react";

/**
 * @deprecated This modal was used for anonymous free trial feature which has been removed.
 * Keeping as an empty component to avoid breaking imports.
 */
export const WelcomeToTrialModal: FunctionComponent = () => {
  return null;
};
