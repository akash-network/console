[OUTPUT]
    Name            datadog
    Match           k8s.*
    Host            http-intake.logs.${DD_SITE}
    TLS             On
    apikey          ${DD_API_KEY}
    dd_source       akash.network
    dd_message_key  message
