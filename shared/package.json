{
  "name": "cloudmos-shared",
  "version": "1.0.0",
  "description": "Code that is shared between multiple cloudmos project",
  "author": "Cloudmos",
  "license": "Apache-2.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^12.0.4",
    "sequelize": "^6.21.3",
    "sequelize-typescript": "^2.1.5"
  }
}
